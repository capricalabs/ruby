		<div id="forgotbox-text">
<%=
	if resource.errors[:password].size > 0
		"Password "+resource.errors[:password][0]
	else
		"Change your password."
	end
%>
		</div>
		<!--  start forgot-inner -->
		<div id="forgot-inner">
			<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>
			<%= f.hidden_field :reset_password_token %>
			<table border="0" cellpadding="0" cellspacing="0">
			<tr>
				<th>New password:</th>
				<td><%= f.password_field :password, :class => 'login-inp' %></td>
			</tr>
			<tr>
				<th>Confirm new password:</th>
				<td><%= f.password_field :password_confirmation, :class => 'login-inp' %></td>
			</tr>
			<tr>
				<th> </th>
				<td><%= f.submit "Change my password", :class => 'submit-login' %></td>
			</tr>
			</table>
			<% end %>
		</div>
		<!--  end forgot-inner -->
		<div class="clear"></div>
		<%= link_to 'Back to login', new_admin_session_path, :class => 'back-login' %>
